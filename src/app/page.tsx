import SearchBar from "./components/SearchBar";
import Map from "./components/Map";

const page = () => {
  return (
    <div>
      <Map />
      <SearchBar />
    </div>
  );
};

export default page;
